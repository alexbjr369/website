---
id: section-01
title: Ignite React.js · Chapter 01 · Section 01
sidebar_label: Section 01
description: Ignite React.js by RocketSeat.
keywords:
  - Alex Bleggi
image: /static/other/img/profile.png
slug: /ignite/react-js/course/chapter-01/section-01
toc_max_heading_level: 4
hide_title: true
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# Configurando ambiente

## [Introdução do módulo](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/introducao-do-modulo)

## [Criando estrutura do projeto](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/criando-estrutura-do-projeto)

**Installing React.js Ecosystem**

- **[`react`](https://www.npmjs.com/package/react)**
- **[`react-dom`](https://www.npmjs.com/package/react-dom)**

```bash
# npm
npm i react react-dom

# yarn
yarn add react react-dom
```

**Folder/File Structure**

```txt {1} title=".gitignore"
node_modules
```

```html {1-10} title="public/index.html"
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Github Explorer</title>
  </head>
  <body></body>
</html>
```

## [Configurando Babel](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/configurando-babel-2)

**Installing Babel/React.js Ecosystem**

- **[`@babel/core`](https://www.npmjs.com/package/@babel/core)**
- **[`@babel/cli`](https://www.npmjs.com/package/@babel/cli)**
- **[`@babel/preset-env`](https://www.npmjs.com/package/@babel/preset-env)**
- **[`@babel/preset-react`](https://www.npmjs.com/package/@babel/preset-react)**

```bash
# npm
npm i -D @babel/core @babel/cli @babel/preset-env @babel/preset-react

# yarn
yarn add -D @babel/core @babel/cli @babel/preset-env @babel/preset-react
```

**Configuring Babel/React.js**

```txt {2} title=".gitignore"
node_modules
dist
```

```javascript {1-3} title="babel.config.js"
module.exports = {
  presets: ['@babel/preset-env', '@babel/preset-react'],
};
```

```javascript {1-5} title="src/index.jsx"
import React from 'react';

function App() {
  return <h1>Hello World</h1>;
}
```

**Running Babel**

```bash
# npm
npx babel src/index.jsx --out-file dist/bundle.js

# yarn
yarn babel src/index.jsx --out-file dist/bundle.js
```

## [Configurando Webpack](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/configurando-webpack-2)

**Installing Webpack Ecosystem**

- **[`webpack`](https://www.npmjs.com/package/webpack)**
- **[`webpack-cli`](https://www.npmjs.com/package/webpack-cli)**
- **[`babel-loader`](https://www.npmjs.com/package/babel-loader)**

```bash
# npm
npm i -D webpack webpack-cli babel-loader

# yarn
yarn add -D webpack webpack-cli babel-loader
```

**Configuring Webpack**

```javascript {1-21} title="webpack.config.js"
const path = require('path');

module.exports = {
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

```javascript {1-3} title="src/App.jsx"
export function App() {
  return <h1>Hello World</h1>;
}
```

```javascript {1-2} title="src/index.jsx"
import React from 'react';
import { App } from './App';
```

**Running Webpack**

```bash
# npm
npx webpack

# yarn
yarn webpack
```

## [Estrutura do React.js](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/estrutura-do-react-js)

```html {10,11} title="public/index.html"
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Github Explorer</title>
  </head>
  <body>
    <div id="root"></div>
    <script src="../dist/bundle.js"></script>
  </body>
</html>
```

```javascript {1,4} title="src/index.jsx"
import { render } from 'react-dom';
import { App } from './App';

render(<App />, document.getElementById('root'));
```

```javascript {6-8} title="babel.config.js"
module.exports = {
  presets: [
    '@babel/preset-env',
    [
      '@babel/preset-react',
      {
        runtime: 'automatic',
      },
    ],
  ],
};
```

```javascript {4} title="webpack.config.js"
const path = require('path');

module.exports = {
  mode: 'development',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

## [Servindo HTML estático](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/servindo-html-estatico)

- **[`html-webpack-plugin`](https://www.npmjs.com/package/html-webpack-plugin)**

```bash
# npm
npm i -D html-webpack-plugin

# yarn
yarn add -D html-webpack-plugin
```

```javascript {2,14-18} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  mode: 'development',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

```diff title="public/index.html"
  <!DOCTYPE html>
  <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Github Explorer</title>
    </head>
    <body>
      <div id="root"></div>
-     <script src="../dist/bundle.js"></script>
    </body>
  </html>
```

## [Webpack Dev Server](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/webpack-dev-server-1)

- **[`webpack-dev-server`](https://www.npmjs.com/package/webpack-dev-server)**

```bash
# npm
npm i -D webpack-dev-server

# yarn
yarn add -D webpack-dev-server
```

```javascript {14-16} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  mode: 'development',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: {
    static: path.resolve(__dirname, 'public'),
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

**Running Webpack**

```bash
# npm
npx webpack serve

# yarn
yarn webpack serve
```

## [Utilizando source maps](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/utilizando-source-maps)

```javascript {6} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  mode: 'development',
  devtool: 'eval-source-map',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: {
    static: path.resolve(__dirname, 'public'),
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

## [Ambiente dev e produção](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/ambiente-dev-e-producao)

- **[`cross-env`](https://www.npmjs.com/package/cross-env)**

```bash
# npm
npm i -D cross-env

# yarn
yarn add -D cross-env
```

```javascript {4,7,8} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

const isDevelopment = process.env.NODE_ENV !== 'production';

module.exports = {
  mode: isDevelopment ? 'development' : 'production',
  devtool: isDevelopment ? 'eval-source-map' : 'source-map',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: {
    static: path.resolve(__dirname, 'public'),
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
    ],
  },
};
```

```json {6-9} title="package.json"
{
  "name": "github-explorer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "webpack serve",
    "build": "cross-env NODE_ENV=production webpack"
  }
}
```

## [Importando arquivos CSS](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/importando-arquivos-css)

- **[`style-loader`](https://www.npmjs.com/package/style-loader)**
- **[`css-loader`](https://www.npmjs.com/package/css-loader)**

```bash
# npm
npm i -D style-loader css-loader

# yarn
yarn add -D style-loader css-loader
```

```css {1-11} title="src/styles/global.css"
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font: Arial, helvetica, sans-serif;
  background: #333;
  color: #fff;
}
```

```javascript {1} title="src/App.jsx"
import './styles/global.css';

export function App() {
  return <h1>Hello World</h1>;
}
```

```javascript {32-36} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

const isDevelopment = process.env.NODE_ENV !== 'production';

module.exports = {
  mode: isDevelopment ? 'development' : 'production',
  devtool: isDevelopment ? 'eval-source-map' : 'source-map',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: {
    static: path.resolve(__dirname, 'public'),
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
      {
        test: /\.css$/,
        exclude: /node_modules/,
        use: ['style-loader', 'css-loader'],
      },
    ],
  },
};
```

## [Utilizando SASS](https://app.rocketseat.com.br/node/chapter-i-1/group/configurando-ambiente/lesson/utilizando-sass)

- **[`sass`](https://www.npmjs.com/package/sass)**
- **[`sass-loader`](https://www.npmjs.com/package/sass-loader)**

```bash
# npm
npm i -D sass sass-loader

# yarn
yarn add -D sass sass-loader
```

```scss {1-11} title="src/styles/global.scss"
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font: Arial, helvetica, sans-serif;
  background: #333;
  color: #fff;
}
```

```javascript {1} title="src/App.jsx"
import './styles/global.scss';

export function App() {
  return <h1>Hello World</h1>;
}
```

```javascript {33,35} title="webpack.config.js"
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

const isDevelopment = process.env.NODE_ENV !== 'production';

module.exports = {
  mode: isDevelopment ? 'development' : 'production',
  devtool: isDevelopment ? 'eval-source-map' : 'source-map',
  entry: path.resolve(__dirname, 'src', 'index.jsx'),
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
  },
  resolve: {
    extensions: ['.js', '.jsx'],
  },
  devServer: {
    static: path.resolve(__dirname, 'public'),
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: path.resolve(__dirname, 'public', 'index.html'),
    }),
  ],
  module: {
    rules: [
      {
        test: /\.jsx$/,
        exclude: /node_modules/,
        use: 'babel-loader',
      },
      {
        test: /\.scss$/,
        exclude: /node_modules/,
        use: ['style-loader', 'css-loader', 'sass-loader'],
      },
    ],
  },
};
```
