---
id: section-03
title: Ignite React.js · Chapter 03 · Section 03
sidebar_label: Section 03
description: Ignite React.js by RocketSeat.
keywords:
  - Alex Bleggi
image: /static/other/img/profile.png
slug: /ignite/react-js/course/chapter-03/section-03
toc_max_heading_level: 4
hide_title: true
---

import useBaseUrl from "@docusaurus/useBaseUrl";

# Integração com API

## [Configurando Stripe](https://app.rocketseat.com.br/node/fundamentos-do-next-js/group/integracao-com-api/lesson/configurando-stripe)

- **[Stripe](https://stripe.com/en-gb-br)**

```txt title=".env.local"
STRIPE_API_KEY=...
```

## [Consumindo API do Stripe (SSR)](https://app.rocketseat.com.br/node/fundamentos-do-next-js/group/integracao-com-api/lesson/consumindo-api-do-stripe-ssr)

```bash
# npm
npm i stripe

# yarn
yarn add stripe
```

```typescript {3-5} title="src/components/SubscribeButton/index.tsx"
import styles from './styles.module.scss';

interface SubscribeButtonProps {
  priceId: string;
}

export function SubscribeButton({ priceId }: SubscribeButtonProps) {
  return (
    <button className={styles.subscribeButton} type="button">
      Subscribe now
    </button>
  );
}
```

```typescript {1,4,7-12,14,28,30,39-56} title="src/pages/index.tsx"
import { GetServerSideProps } from 'next';
import Head from 'next/head';
import { SubscribeButton } from '../components/SubscribeButton';
import { stripe } from '../services/stripe';
import styles from './home.module.scss';

interface HomeProps {
  product: {
    priceId: string,
    amount: number
  }
}

export default function Home({ product }: HomeProps) {
  return (
    <>
      <Head>
        <title>Home | ig.news</title>
      </Head>
      <main className={styles.contentContainer}>
        <section className={styles.hero}>
          <span>👏 Hey, welcome</span>
          <h1>
            News about the <span>React</span> world.
          </h1>
          <p>
            Get access to all the publications <br />
            <span>for {product.amount} month</span>
          </p>
          <SubscribeButton priceId={product.priceId} />
        </section>

        <img src="images/avatar.svg" alt="Girl coding" />
      </main>
    </>
  );
}

// o nome da função tem que ser este
export const getServerSideProps: GetServerSideProps = async () => {
  const price = await stripe.prices.retrieve('price_1KlVTIIYWJkapL3ipt4RuopL');

  const product = {
    priceId: price.id,
    amount: new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: 'USD'
    }).format(price.unit_amount / 100), // para salvar em centavos
  }

  return {
    props: {
      product
    }
  }
}
```

```typescript {1-12} title="src/services/stripe.ts"
import Stripe from 'stripe';
import { version } from '../../package.json'

export const stripe = new Stripe(
  process.env.STRIPE_API_KEY, {
    apiVersion: '2020-08-27',
    appInfo: {
      name: 'Ignews',
      version
    }
  }
);
```

## [Static Site Generation (SSG)](https://app.rocketseat.com.br/node/fundamentos-do-next-js/group/integracao-com-api/lesson/static-site-generation-ssg)

**3 formas de fazer chamadas**

- **client-side:** quando não há necessidade de indexação pelo google, uqando uma informação é carregada através de uma ação do usuario
- **server-side:** onde há necessidade de indexação pelo google e dados dinâmicos da sessão do usuário
- **static site generation:** apenas para páginas onde o conteudo é o mesmo para todos usuarios e onde há necessidade de indexação pelo google

```typescript {1,40,55} title="src/pages/index.tsx"
import { GetStaticProps } from 'next';
import Head from 'next/head';
import { SubscribeButton } from '../components/SubscribeButton';
import { stripe } from '../services/stripe';
import styles from './home.module.scss';

interface HomeProps {
  product: {
    priceId: string,
    amount: number
  }
}

export default function Home({ product }: HomeProps) {
  return (
    <>
      <Head>
        <title>Home | ig.news</title>
      </Head>
      <main className={styles.contentContainer}>
        <section className={styles.hero}>
          <span>👏 Hey, welcome</span>
          <h1>
            News about the <span>React</span> world.
          </h1>
          <p>
            Get access to all the publications <br />
            <span>for {product.amount} month</span>
          </p>
          <SubscribeButton priceId={product.priceId} />
        </section>

        <img src="images/avatar.svg" alt="Girl coding" />
      </main>
    </>
  );
}

// o nome da função tem que ser este
export const getStaticProps: GetStaticProps = async () => {
  const price = await stripe.prices.retrieve('price_1KlVTIIYWJkapL3ipt4RuopL');

  const product = {
    priceId: price.id,
    amount: new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: 'USD'
    }).format(price.unit_amount / 100), // para salvar em centavos
  }

  return {
    props: {
      product
    },
    revalidate: 60 * 60 * 24, // 24 hours
  }
}
```
