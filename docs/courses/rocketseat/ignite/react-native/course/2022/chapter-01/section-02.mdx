---
id: section-02
title: Ignite React Native · 2022 · Chapter 01 · Section 02
sidebar_label: Section 02
description: Ignite React Native by RocketSeat.
keywords:
  - Alex Bleggi
image: /static/other/img/profile.png
slug: /ignite/react-native/course/2022/chapter-01/section-02
toc_max_heading_level: 4
hide_title: true
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# Fundamentos

## [Preparando seu Ambiente de Desenvolvimento](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/preparando-seu-ambiente-de-desenvolvimento)

- **[Configuração](https://react-native.rocketseat.dev/)**

## [Expo com NPX](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/expo-com-npx)

## [Criando novo projeto](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/criando-novo-projeto-1)

```bash
npx create-expo-app --template
```

```bash
$ npx create-expo-app --template
√ Choose a template: » Blank (TypeScript)
√ What is your app named? ... imhere
```

## [Estrutura de pastas e arquivos](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/estrutura-de-pastas-e-arquivos-3)

## [Executando o projeto](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/executando-o-projeto-1)

```bash
npx expo start
```

## [Funcionamento do React Native](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/funcionamento-do-react-native)

## [Refresh e Reload](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/refresh-e-reload)

## [Entendendo o JSX](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/entendendo-o-jsx)

```tsx {5,8} title="App.tsx"
import { Text } from "react-native";

export default function App() {
  return (
    <>
      <Text>React</Text>
      <Text>Native</Text>
    </>
  );
}
```

```tsx {1,6,9} title="App.tsx"
import { Fragment } from "react";
import { Text } from "react-native";

export default function App() {
  return (
    <Fragment>
      <Text>React</Text>
      <Text>Native</Text>
    </Fragment>
  );
}
```

```tsx {1,5,8} title="App.tsx"
import { Text, View } from "react-native";

export default function App() {
  return (
    <View>
      <Text>React</Text>
      <Text>Native</Text>
    </View>
  );
}
```

## [Entendendo a LogBox](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/entendendo-a-log-box)

## [Estilizando Elementos](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/estilizando-elementos)

```tsx {6-10,13-18,20,23-26,28} title="App.tsx"
import { Text, View } from 'react-native';

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: '#131016',
        padding: 24,
      }}
    >
      <Text
        style={{
          color: '#fdfcfe',
          fontSize: 24,
          fontWeight: 'bold',
          marginTop: 48,
        }}
      >
        Nome do evento
      </Text>
      <Text
        style={{
          color: '#6b6b6b',
          fontSize: 16,
        }}
      >
        Sexta, 4 de Novembro de 2022.
      </Text>
    </View>
  );
}
```

## [Entendendo densidade de pixel](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/entendendo-densidade-de-pixel)

## [Utilizando StyleSheet](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/utilizando-style-sheet-1)

```tsx {1,5-8,12-28} title="App.tsx"
import { Text, View, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fdfcfe',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
});
```

## [Organizando o projeto](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/organizando-o-projeto-1)

```ts title="src/screens/Home/styles.ts"
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fdfcfe',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
});
```

```tsx title="src/screens/Home/index.tsx"
import { Text, View } from 'react-native';
import { styles } from './styles';

export default function Home() {
  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
    </View>
  );
}
```

```tsx {1,5-8,12-28} title="App.tsx"
import { Text, View, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fdfcfe',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
});
```

```tsx {1,4} title="App.tsx"
import Home from './src/screens/Home';

export default function App() {
  return <Home />;
}
```

## [Export e Export Default](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/export-e-export-default)

**Default**

```tsx {1} title="src/screens/Home/index.tsx"
export default function Home() {
}
```

```tsx {1} title="App.tsx"
import Batata from './src/screens/Home';
```

**Named**

```tsx {1} title="src/screens/Home/index.tsx"
export function Home() { }
```

```tsx {1} title="App.tsx"
import { Home } from './src/screens/Home';
```

**Default**

```tsx {1} title="src/screens/Home/index.tsx"
export CONFIG = '';

export default function Home() { }
```

```tsx {1} title="App.tsx"
import Home, { Config } from './src/screens/Home';
```

**Named**

```tsx {1} title="src/screens/Home/index.tsx"
export CONFIG = '';

export function Home() { }
```

```tsx {1} title="App.tsx"
import { Home, CONFIG } from './src/screens/Home';
```

## [Input de dados](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/input-de-dados)

```ts {10,19-25} title="src/screens/Home/styles.ts"
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fff',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
  input: {
    height: 56,
    backgroundColor: '#1f1e25',
    borderRadius: 5,
    color: '#fff',
    padding: 16,
  },
});
```

```tsx {9-14} title="src/screens/Home/index.tsx"
import { Text, View, TextInput } from 'react-native';
import { styles } from './styles';

export function Home() {
  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <TextInput
        style={styles.input}
        placeholder="Nome do participante"
        placeholderTextColor="#6b6b6b"
        keyboardType="numeric"
      />
    </View>
  );
}
```

## [Botão de Adicionar](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/botao-de-adicionar)

```ts {26-37} title="src/screens/Home/styles.ts"
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fff',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
  input: {
    height: 56,
    backgroundColor: '#1f1e25',
    borderRadius: 5,
    color: '#fff',
    padding: 16,
  },
  buttonText: {
    color: '#fff',
    fontSize: 24,
  },
  button: {
    width: 56,
    height: 56,
    borderRadius: 5,
    backgroundColor: '#31cf67',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

```tsx {5-7,19-21} title="src/screens/Home/index.tsx"
import { Text, View, TextInput, TouchableOpacity } from 'react-native';
import { styles } from './styles';

export function Home() {
  function handleParticipantAdd() {
    console.log('clicked!');
  }

  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <TextInput
        style={styles.input}
        placeholder="Nome do participante"
        placeholderTextColor="#6b6b6b"
        keyboardType="numeric"
      />
      <TouchableOpacity style={styles.button} onPress={handleParticipantAdd}>
        <Text style={styles.buttonText}>+</Text>
      </TouchableOpacity>
    </View>
  );
}
```

## [Criando o Form](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/criando-o-form)

```ts {15-20,31,32} title="src/screens/Home/styles.ts"
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#131016',
    padding: 24,
  },
  eventName: {
    color: '#fff',
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 48,
  },
  form: {
    width: '100%',
    flexDirection: 'row',
    marginTop: 36,
    marginBottom: 42,
  },
  eventDate: {
    color: '#6b6b6b',
    fontSize: 16,
  },
  input: {
    height: 56,
    backgroundColor: '#1f1e25',
    borderRadius: 5,
    color: '#fff',
    padding: 16,
    flex: 1,
    marginRight: 12,
  },
  buttonText: {
    color: '#fff',
    fontSize: 24,
  },
  button: {
    width: 56,
    height: 56,
    borderRadius: 5,
    backgroundColor: '#31cf67',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

```tsx {13,23} title="src/screens/Home/index.tsx"
import { Text, View, TextInput, TouchableOpacity } from 'react-native';
import { styles } from './styles';

export function Home() {
  function handleParticipantAdd() {
    console.log('clicked!');
  }

  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <View style={styles.form}>
        <TextInput
          style={styles.input}
          placeholder="Nome do participante"
          placeholderTextColor="#6b6b6b"
          keyboardType="numeric"
        />
        <TouchableOpacity style={styles.button} onPress={handleParticipantAdd}>
          <Text style={styles.buttonText}>+</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}
```

## [Entendendo Componetização](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/criando-o-form)

## [Criando Componente Participant](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/criando-componente-participant)

```ts title="src/components/Participant/styles.ts"
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: '#1f1e25',
    borderRadius: 5,
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 12,
  },
  name: {
    flex: 1,
    fontSize: 16,
    color: '#fff',
    marginLeft: 16,
  },
  buttonText: {
    color: '#fff',
    fontSize: 24,
  },
  button: {
    width: 56,
    height: 56,
    borderRadius: 5,
    backgroundColor: '#e23c44',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

```tsx title="src/components/Participant/index.tsx"
import { Text, TouchableOpacity, View } from 'react-native';
import { styles } from './styles';

export function Participant() {
  return (
    <View style={styles.container}>
      <Text style={styles.name}>Nome do evento</Text>
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>-</Text>
      </TouchableOpacity>
    </View>
  );
}
```

```tsx {2,25,26} title="src/screens/Home/index.tsx"
import { Text, TextInput, TouchableOpacity, View } from 'react-native';
import { Participant } from '../../components/Participant/index';
import { styles } from './styles';

export function Home() {
  function handleParticipantAdd() {
    console.log('clicked!');
  }

  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <View style={styles.form}>
        <TextInput
          style={styles.input}
          placeholder="Nome do participante"
          placeholderTextColor="#6b6b6b"
          keyboardType="numeric"
        />
        <TouchableOpacity style={styles.button} onPress={handleParticipantAdd}>
          <Text style={styles.buttonText}>+</Text>
        </TouchableOpacity>
      </View>
      <Participant />
      <Participant />
    </View>
  );
}
```

## [Entendendo o Typescript](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/entendendo-o-typescript)

## [Utilizando Propriedades no Componente](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/utilizando-propriedades-no-componente)

```tsx {4-6,8,11} title="src/components/Participant/index.tsx"
import { Text, TouchableOpacity, View } from 'react-native';
import { styles } from './styles';

type Props = {
  name: string;
};

export function Participant({ name }: Props) {
  return (
    <View style={styles.container}>
      <Text style={styles.name}>{name}</Text>
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>-</Text>
      </TouchableOpacity>
    </View>
  );
}
```

```tsx {25,26,27,28} title="src/screens/Home/index.tsx"
import { Text, TextInput, TouchableOpacity, View } from 'react-native';
import { Participant } from '../../components/Participant/index';
import { styles } from './styles';

export function Home() {
  function handleParticipantAdd() {
    console.log('clicked!');
  }

  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <View style={styles.form}>
        <TextInput
          style={styles.input}
          placeholder="Nome do participante"
          placeholderTextColor="#6b6b6b"
          keyboardType="numeric"
        />
        <TouchableOpacity style={styles.button} onPress={handleParticipantAdd}>
          <Text style={styles.buttonText}>+</Text>
        </TouchableOpacity>
      </View>
      <Participant name="John" />
      <Participant name="John 2" />
      <Participant name="John 3" />
      <Participant name="John 4" />
    </View>
  );
}
```

## [Função Remover](https://app.rocketseat.com.br/node/projeto-01/group/fundamentos-2/lesson/funcao-remover)

```tsx {6,9,13} title="src/components/Participant/index.tsx"
import { Text, TouchableOpacity, View } from 'react-native';
import { styles } from './styles';

type Props = {
  name: string;
  onRemove: () => void;
};

export function Participant({ name, onRemove }: Props) {
  return (
    <View style={styles.container}>
      <Text style={styles.name}>{name}</Text>
      <TouchableOpacity style={styles.button} onPress={onRemove}>
        <Text style={styles.buttonText}>-</Text>
      </TouchableOpacity>
    </View>
  );
}
```

```tsx {10-12,29-32} title="src/screens/Home/index.tsx"
import { Text, TextInput, TouchableOpacity, View } from 'react-native';
import { Participant } from '../../components/Participant/index';
import { styles } from './styles';

export function Home() {
  function handleParticipantAdd() {
    console.log('clicked!');
  }

  function handleParticipantRemove(name: string) {
    console.log(`${name} removed!`);
  }

  return (
    <View style={styles.container}>
      <Text style={styles.eventName}>Nome do evento</Text>
      <Text style={styles.eventDate}>Sexta, 4 de Novembro de 2022.</Text>
      <View style={styles.form}>
        <TextInput
          style={styles.input}
          placeholder="Nome do participante"
          placeholderTextColor="#6b6b6b"
          keyboardType="numeric"
        />
        <TouchableOpacity style={styles.button} onPress={handleParticipantAdd}>
          <Text style={styles.buttonText}>+</Text>
        </TouchableOpacity>
      </View>
      <Participant
        name="John"
        onRemove={() => handleParticipantRemove('John')}
      />
    </View>
  );
}
```
