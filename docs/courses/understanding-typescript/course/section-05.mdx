---
id: section-05
title: Understanding TypeScript · Section 05
sidebar_label: Section 05
description: A study of the course Understanding TypeScript Course by Academind.
keywords:
  - Alex Bleggi
image: /static/other/img/profile.png
slug: /understanding-typescript/course/section-05
---

import useBaseUrl from "@docusaurus/useBaseUrl";

# Classes & Interfaces

## [Module Introduction](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888240#overview)

## [What are Classes?](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888244#overview)

## [Creating a First Class](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888246#overview)

```typescript title="app.ts"
class Department {
  name: string;

  constructor(name: string) {
    this.name = name;
  }
}

const accounting = new Department('Accounting');
```

## [Compiling to JavaScript](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888248#overview)

## [Constructor Functions & The "this" Keyword](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888250#overview)

```typescript {8-10,13,14} title="app.ts"
class Department {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  describe() {
    console.log(`Department: ${this.name}`);
  }
}

const accounting = new Department('Accounting');
accounting.describe(); // result: 'Department: Accounting'
```

```typescript {16,17} title="app.ts"
class Department {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  describe() {
    console.log(`Department: ${this.name}`);
  }
}

const accounting = new Department('Accounting');
accounting.describe();

const accountingCopy = { describe: accounting.describe }; // this refers to the object that is calling the method
accountingCopy.describe(); // result: Department: undefined
```

```typescript {8,17} title="app.ts"
class Department {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  describe(this: Department) {
    console.log(`Department: ${this.name}`);
  }
}

const accounting = new Department('Accounting');
accounting.describe(); // result: Department: Accounting

const accountingCopy = { describe: accounting.describe }; // this refers to the object that is calling the method
accountingCopy.describe(); // error
```

```typescript {16,17} title="app.ts"
class Department {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  describe(this: Department) {
    console.log(`Department: ${this.name}`);
  }
}

const accounting = new Department('Accounting');
accounting.describe(); // result: Department: Accounting

const accountingCopy = { name: 'Dummy', describe: accounting.describe }; // this refers to the object that is calling the method
accountingCopy.describe(); // result: Department: Dummy
```

## ["private" and "public" Access Modifiers](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888254#overview)

```typescript {3,28} title="app.ts"
class Department {
  name: string;
  employees: string[] = [];

  constructor(name: string) {
    this.name = name;
  }

  describe(this: Department) {
    console.log(`Department: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

const accounting = new Department('Accounting');

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.employees[2] = 'John 3'; // not a good practice

accounting.printEmployeeInformation();
accounting.describe();
```

```typescript {3,28} title="app.ts"
class Department {
  name: string;
  private employees: string[] = [];

  constructor(name: string) {
    this.name = name;
  }

  describe(this: Department) {
    console.log(`Department: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

const accounting = new Department('Accounting');

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.employees[2] = 'John 3'; // not accessible anymore

accounting.printEmployeeInformation();
accounting.describe();
```

## [Shorthand Initialization](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888258#overview)

```typescript {4,6-8} title="app.ts"
class Department {
  private employees: string[] = [];

  constructor(private id: string, public name: string) { }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

const accounting = new Department('id', 'Accounting');

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.printEmployeeInformation();
accounting.describe();
```

## ["readonly" Properties](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888260#overview)

"readonly" are properties that should not change.

```typescript {4} title="app.ts"
class Department {
  private employees: string[] = [];

  constructor(private readonly id: string, public name: string) { }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

const accounting = new Department('id', 'Accounting');

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.printEmployeeInformation();
accounting.describe();
```

## [Quiz 4: Class Basics](https://www.udemy.com/course/understanding-typescript/learn/quiz/4745148#overview)

## [Inheritance](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888264#overview)

```typescript {20-24,26-38,40,42,43,45,46,48,50,52} title="app.ts"
class Department {
  private employees: string[] = [];

  constructor(private readonly id: string, public name: string) { }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }
}

class AccountingDepartment extends Department {
  constructor(id: string, public reports: string[]) {
    super(id, 'Accounting'); // forwarding to the constructor of Department
  }

  addReport(text: string) {
    this.reports.push(text);
  }

  printReports() {
    console.log(this.reports);
  }
}

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = new AccountingDepartment('id', []);

accounting.addReport('Something went wrong...');

accounting.printReports();
```

## [Overriding Properties & The "protected" Modifier](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888274#overview)

```typescript {2,31-37,60,61,64} title="app.ts"
class Department {
  protected employees: string[] = [];

  constructor(private readonly id: string, public name: string) { }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }
}

class AccountingDepartment extends Department {
  constructor(id: string, public reports: string[]) {
    super(id, 'Accounting'); // forwarding to the constructor of Department
  }

  addEmployee(name: string) {
    if (name === 'John') {
      return;
    }

    this.employees.push(name)
  }

  addReport(text: string) {
    this.reports.push(text);
  }

  printReports() {
    console.log(this.reports);
  }
}

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = new AccountingDepartment('id', []);

accounting.addReport('Something went wrong...');

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.printReports();
accounting.printEmployeeInformation();
```

## [Getters & Setters](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888278#overview)

```typescript {27,29-34,36-41,45,58,76,79} title="app.ts"
class Department {
  protected employees: string[] = [];

  constructor(private readonly id: string, public name: string) { }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }
}

class AccountingDepartment extends Department {
  private lastReport: string;

  get mostRecentReport() {
    if (this.lastReport) {
      return this.lastReport;
    }
    throw new Error('No report found.')
  }

  set mostRecentReport(value: string) {
    if (!value) {
      throw new Error('please pass in a valid value!');
    }
    this.addReport(value);
  }

  constructor(id: string, public reports: string[]) {
    super(id, 'Accounting'); // forwarding to the constructor of Department
    this.lastReport = reports[0];
  }

  addEmployee(name: string) {
    if (name === 'John') {
      return;
    }

    this.employees.push(name)
  }

  addReport(text: string) {
    this.reports.push(text);
    this.lastReport = text;
  }

  printReports() {
    console.log(this.reports);
  }
}

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = new AccountingDepartment('id', []);

accounting.mostRecentReport = 'Year end report'; // setter: access as a property
accounting.addReport('Something went wrong...');

console.log(accounting.mostRecentReport); // getter: access as a property

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.printReports();
accounting.printEmployeeInformation();
```

## [Static Methods & Properties](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888280#overview)

Static methods and properties are not accessible on the instance of the class.

```typescript {2,7,8,11-13,75,76} title="app.ts"
class Department {
  static fiscalYear = 2021;

  protected employees: string[] = [];

  constructor(private readonly id: string, public name: string) {
    console.log(this.fiscalYear); // not possible to access static member in non static members
    console.log(Department.fiscalYear); // to access static member in non static members
  }

  static createEmployee(name: string) {
    return { name };
  }

  describe(this: Department) {
    console.log(`Department ${this.id}: ${this.name}`);
  }

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }
}

class AccountingDepartment extends Department {
  private lastReport: string;

  get mostRecentReport() {
    if (this.lastReport) {
      return this.lastReport;
    }
    throw new Error('No report found.')
  }

  set mostRecentReport(value: string) {
    if (!value) {
      throw new Error('please pass in a valid value!');
    }
    this.addReport(value);
  }

  constructor(id: string, public reports: string[]) {
    super(id, 'Accounting'); // forwarding to the constructor of Department
    this.lastReport = reports[0];
  }

  addEmployee(name: string) {
    if (name === 'John') {
      return;
    }

    this.employees.push(name)
  }

  addReport(text: string) {
    this.reports.push(text);
    this.lastReport = text;
  }

  printReports() {
    console.log(this.reports);
  }
}

const employee = Department.createEmployee('John');
console.log(employee, Department.fiscalYear); // result: { name: 'John' } 2021

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = new AccountingDepartment('id', []);

accounting.mostRecentReport = 'Year end report'; // setter: access as a property
accounting.addReport('Something went wrong...');

console.log(accounting.mostRecentReport); // getter: access as a property

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.printReports();
accounting.printEmployeeInformation();
```

## [Abstract Classes](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888284#overview)

```typescript {1,12,29-31,99} title="app.ts"
abstract class Department {
  static fiscalYear = 2021;

  protected employees: string[] = [];

  constructor(protected readonly id: string, public name: string) {}

  static createEmployee(name: string) {
    return { name };
  }

  abstract describe(this: Department): void;

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }

  describe() {
    console.log(`IT Department - id: ${this.id}`);
  }
}

class AccountingDepartment extends Department {
  private lastReport: string;

  get mostRecentReport() {
    if (this.lastReport) {
      return this.lastReport;
    }
    throw new Error('No report found.')
  }

  set mostRecentReport(value: string) {
    if (!value) {
      throw new Error('please pass in a valid value!');
    }
    this.addReport(value);
  }

  constructor(id: string, public reports: string[]) {
    super(id, 'Accounting');
    this.lastReport = reports[0];
  }

  describe() {
    console.log(`Accounting Department id: ${this.id}`);
  }

  addEmployee(name: string) {
    if (name === 'John') {
      return;
    }

    this.employees.push(name)
  }

  addReport(text: string) {
    this.reports.push(text);
    this.lastReport = text;
  }

  printReports() {
    console.log(this.reports);
  }
}

const employee = Department.createEmployee('John');
console.log(employee, Department.fiscalYear);

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = new AccountingDepartment('id', []);

accounting.mostRecentReport = 'Year end report';
accounting.addReport('Something went wrong...');

console.log(accounting.mostRecentReport);

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.describe();
```

## [Singletons & Private Constructors](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888290#overview)

```typescript {36,52,57-63,98} title="app.ts"
abstract class Department {
  static fiscalYear = 2021;

  protected employees: string[] = [];

  constructor(protected readonly id: string, public name: string) {}

  static createEmployee(name: string) {
    return { name };
  }

  abstract describe(this: Department): void;

  addEmployee(employee: string) {
    this.employees.push(employee);
  }

  printEmployeeInformation() {
    console.log(this.employees.length);
    console.log(this.employees);
  }
}

class ITDepartment extends Department {
  constructor(id: string, public admins: string[]) {
    super(id, 'IT'); // forwarding to the constructor of Department
  }

  describe() {
    console.log(`IT Department - id: ${this.id}`);
  }
}

class AccountingDepartment extends Department {
  private lastReport: string;
  private static instance:AccountingDepartment;

  get mostRecentReport() {
    if (this.lastReport) {
      return this.lastReport;
    }
    throw new Error('No report found.')
  }

  set mostRecentReport(value: string) {
    if (!value) {
      throw new Error('please pass in a valid value!');
    }
    this.addReport(value);
  }

  private constructor(id: string, public reports: string[]) {
    super(id, 'Accounting');
    this.lastReport = reports[0];
  }

  static getInstance() {
    if (this.instance) {
      return this.instance;
    }
    this.instance = new AccountingDepartment('id', []);
    return this.instance;
  }

  describe() {
    console.log(`Accounting Department id: ${this.id}`);
  }

  addEmployee(name: string) {
    if (name === 'John') {
      return;
    }

    this.employees.push(name)
  }

  addReport(text: string) {
    this.reports.push(text);
    this.lastReport = text;
  }

  printReports() {
    console.log(this.reports);
  }
}

const employee = Department.createEmployee('John');
console.log(employee, Department.fiscalYear);

const it = new ITDepartment('id', ['John']);

it.addEmployee('John');
it.addEmployee('John 2');

it.printEmployeeInformation();
it.describe();

const accounting = AccountingDepartment.getInstance(); // we can create only one instance

accounting.mostRecentReport = 'Year end report';
accounting.addReport('Something went wrong...');

console.log(accounting.mostRecentReport);

accounting.addEmployee('John');
accounting.addEmployee('John 2');

accounting.describe();
```

## [Classes - A Summary](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888296#overview)

## [Quiz 5: Classes](https://www.udemy.com/course/understanding-typescript/learn/quiz/4745152#overview)

## [A First Interface](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888306#overview)

Describes the structure of an object.

```typescript title="app.ts"
interface Person {
  name: string;
  age: number;
  greet(phrase: string): void;
}

let user: Person;

user = {
  name: 'John',
  age: 30,
  greet(phrase: string) {
    console.log(phrase);
  }
}

user.greet('Hi there!'); // result: Hi there!
```

## [Using Interfaces with Classes](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888310#overview)

```typescript title="app.ts"
interface Greetable {
  name: string;
  greet(phrase: string): void;
}

class Person implements Greetable {
  constructor(public name: string) {}

  greet(phrase: string) {
    console.log(phrase);
  }
}

const user = new Person('John');
user.greet('Hi there!')
```

## [Why Interfaces?](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888312#overview)

## [Readonly Interface Properties](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888316#overview)

```typescript {2,15} title="app.ts"
interface Greetable {
  readonly name: string;
  greet(phrase: string): void;
}

class Person implements Greetable {
  constructor(public name: string) {}

  greet(phrase: string) {
    console.log(phrase);
  }
}

const user:Greetable = new Person('John');
user.name = 'John'; // error
```

## [Extending Interfaces](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888320#overview)

```typescript {1-3,5-7} title="app.ts"
interface Named {
  readonly name: string;
}

interface Greetable extends Named {
  greet(phrase: string): void;
}

class Person implements Greetable {
  constructor(public name: string) {}

  greet(phrase: string) {
    console.log(phrase);
  }
}

const user:Greetable = new Person('John');
```

## [Interfaces as Function Types](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888326#overview)

```typescript {1-3,5-7} title="app.ts"
interface AddFn {
  (a: number, b: number): number
}

const add: AddFn = (n1: number, n2: number)=> {
  return n1 + n2;
}

interface Named {
  readonly name: string;
}

interface Greetable extends Named {
  greet(phrase: string): void;
}

class Person implements Greetable {
  constructor(public name: string) {}

  greet(phrase: string) {
    console.log(phrase);
  }
}

const user:Greetable = new Person('John');
```

## [Optional Parameters & Properties](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888332#overview)

```typescript {10,11,19} title="app.ts"
interface AddFn {
  (a: number, b: number): number
}

const add: AddFn = (n1: number, n2: number)=> {
  return n1 + n2;
}

interface Named {
  readonly name?: string;
  outputName?: string;
}

interface Greetable extends Named {
  greet(phrase: string): void;
}

class Person implements Greetable {
  constructor(public name?: string) {}

  greet(phrase: string) {
    console.log(phrase);
  }
}

const user:Greetable = new Person('John');
```

## [Compiling Interfaces to JavaScript](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888336#overview)

## [Quiz 6: Interfaces](https://www.udemy.com/course/understanding-typescript/learn/quiz/4745154#overview)

## [Wrap Up](https://www.udemy.com/course/understanding-typescript/learn/lecture/16888340#overview)

## [Useful Resources & Links](https://www.udemy.com/course/understanding-typescript/learn/lecture/16890018#overview)
