---
id: implementation
title: Linting - Code-quality Rules · ESLint · TypeScript · Implementation
sidebar_label: Implementation
description: A compilation of Code-quality Rules Linting configurations.
keywords:
  - Alex Bleggi
image: /static/other/img/profile.png
slug: /projects/linting-code-quality-rules/eslint/typescript/implementation
toc_max_heading_level: 4
hide_title: true
---

import useBaseUrl from "@docusaurus/useBaseUrl";

# Implementation

Step by step **ESLint** configuration for **TypeScript**.

## Formatting Rules Linting

Follow the steps in **[Linting - Formatting Rules](/docs/projects/linting-formatting-rules/implementation)**.

## Commit Linting

Follow the steps in **[Linting - Commit](/docs/projects/linting-commit/implementation)**.

## ESLint

- **[`eslint`](https://www.npmjs.com/package/eslint)**

```bash
# npm
npm i -D eslint

# yarn
yarn add -D eslint
```

##### Plugins

- **[`eslint-config-airbnb-base`](https://www.npmjs.com/package/eslint-config-airbnb-base)**
- **[`eslint-config-prettier`](https://www.npmjs.com/package/eslint-config-prettier)**
- **[`eslint-plugin-import`](https://www.npmjs.com/package/eslint-plugin-import)**
- **[`eslint-plugin-prettier`](https://www.npmjs.com/package/eslint-plugin-prettier)**
- **[`@typescript-eslint/eslint-plugin`](https://www.npmjs.com/package/@typescript-eslint/eslint-plugin)**
- **[`@typescript-eslint/parser`](https://www.npmjs.com/package/@typescript-eslint/parser)**

```bash
# npm
npm i -D eslint-config-airbnb-base eslint-config-prettier eslint-plugin-import eslint-plugin-prettier @typescript-eslint/eslint-plugin @typescript-eslint/parser

# yarn
yarn add -D eslint-config-airbnb-base eslint-config-prettier eslint-plugin-import eslint-plugin-prettier @typescript-eslint/eslint-plugin @typescript-eslint/parser
```

##### Configuration

```json title=".eslintrc.json"
{
  "env": {
    "es2022": true,
    "browser": true
  },
  "parser": "@typescript-eslint/parser",
  "extends": ["airbnb-base", "prettier"],
  "plugins": ["@typescript-eslint", "prettier"],
  "rules": {
    "prettier/prettier": "error"
  }
}
```

```json {10} title="package.json"
{
  "name": "project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "npx husky install",
    "prettier": "prettier . --write --ignore-unknown",
    "commit": "git-cz",
    "eslint": "eslint **/*.ts --fix"
  },
  "lint-staged": {
    "**/*": "npm run prettier",
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
```

## lint-staged

##### Configuration

```json {14} title="package.json"
{
  "name": "eslint-javascript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "npx husky install",
    "prettier": "prettier . --write --ignore-unknown",
    "commit": "git-cz",
    "eslint": "eslint **/*.ts --fix"
  },
  "lint-staged": {
    "**/*": "npm run prettier",
    "**/*.ts": "npm run eslint"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
```

## Testing

```typescript title="src/script-a.ts"
const hello = () => {
  console.log('Hello from script-a.ts');
};

export default hello;
```

```typescript title="src/script-b.ts"
const hello = () => {
  console.log('Hello from script-b.ts');
};

export default hello;
```

```typescript title="src/index.ts"
import * as scriptA from './script-a';
import * as scriptB from './script-b';

scriptA.hello();
scriptB.hello();

const test = [1, 2, 3].map((n) => n + 1);

console.log(test);
```

```bash
# npm
npm run commit

# yarn
yarn commit
```
